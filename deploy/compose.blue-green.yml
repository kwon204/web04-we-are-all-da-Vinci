name: daVinci-deploy

services:
  blue:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: daVinci-blue-server
    ports:
      - "3000:3000"
    env_file: .env
    restart: unless-stopped
    networks:
      - app-network
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3000/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  green:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: daVinci-green-server
    ports:
      - "3001:3000"
    env_file: .env
    restart: unless-stopped
    networks:
      - app-network
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3000/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  app-network:
    driver: bridge
